<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd">

	<!-- 컨트롤러 위치 스캔 -->
	<context:component-scan
		base-package="com.wini.winitest.controller" />
	<!-- Service 스캔 추가 -->
	<context:component-scan
		base-package="com.wini.winitest.service" />

	<!-- MVC 어노테이션 활성화 -->
	<mvc:annotation-driven />

	<!-- JSP 위치 설정 -->
	<bean
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix" value="/WEB-INF/views/" />
		<property name="suffix" value=".jsp" />
	</bean>
	<!-- 파일 업로드 설정 -->
	<bean id="multipartResolver"
	      class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
	    <property name="maxUploadSize" value="109715200"/>
	    <property name="maxInMemorySize" value="1048576"/>
	</bean>
	
	
	<!-- 컨트롤러 전에 실행(필터와 차이) -->
	<mvc:interceptors>
    <mvc:interceptor>
    	<!-- 모든 요청에 인터셉터 적용 -->
        <mvc:mapping path="/**/*.do" />
        <!-- 제외 목록 -->
        <mvc:exclude-mapping path="/user/login.do"/>
        <mvc:exclude-mapping path="/user/register.do"/>
        <mvc:exclude-mapping path="/user/idCheck.do"/>
        <!-- 사용할 인터셉터 클래스 -->
        <bean class="com.wini.winitest.interceptor.LoginInterceptor"/>
    </mvc:interceptor>
	</mvc:interceptors>
</beans>